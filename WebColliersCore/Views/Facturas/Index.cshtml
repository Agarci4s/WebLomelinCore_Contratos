@model WebLomelinCore.Models.FacturasPagadas

<header>
    <script src="~/js/core.js"></script>
    <script src="//code.jquery.com/jquery-1.12.0.min.js"></script>
    <link rel="stylesheet" href="~/lib/font-awesome/css/font-awesome.min.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="https://kit.fontawesome.com/5b1471f5e3.js" crossorigin="anonymous"></script>
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="~/js/sweetalert.min.js"></script>
    <link href="~/css/sweetalert.css" rel="stylesheet">

    <style>

        input::-webkit-inner-spin-button,
        input::-webkit-outer-spin-button {
            -webkit-appearance: none;
        }

        input[type="number"] {
            -moz-appearance: textfield;
        }

        input[type="button"] {
            cursor: pointer;
        }

        input::-webkit-clear-button {
            display: none;
            -webkit-appearance: none;
        }


        .custom-file-input ~ .custom-file-label::after {
            content: "...";
        }

        .tableFixHead {
            overflow-y: auto;
            height: 480px;
        }

            .tableFixHead table {
                border-collapse: collapse;
                width: 100%;
            }

            .tableFixHead th,
            .tableFixHead td {
                padding: 8px 16px;
            }

            .tableFixHead td {
                text-align: center;
            }

            .tableFixHead th {
                position: sticky;
                top: 0;
                /* background-color: white;
                border: 0px solid white; */
                text-align: center;
                background: rgb(37, 64, 143);
                color: white;
            }

        .form-group-custom {
            margin-bottom: 6px;
        }

        .font-size-row {
            font-size: 12px !important;
        }

        .font-size-head {
            font-size: 12px !important;
        }

        .RowNegativo {
            background-color: red;
            border-color: red;
        }

        .RowPositivo {
            background-color: green;
            border-color: green;
        }

        .ColorTextRed {
            color: red;
        }

        .pointer {
            cursor: pointer;
        }


        table tr th {
            cursor: pointer;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        .sorting {
        }

        .asc:after {
            content: ' ↑';
        }

        .desc:after {
            content: " ↓";
        }

    </style>
</header>

<h3>Facturas Cargadas</h3>

<form asp-action="Busqueda" asp-controller="Facturas" id="frmBusqueda" name="frmBusqueda">
    <div class="container-fluid">
        <div class="card">
            <div class="card-header">
                <h6 class="card-title m-1">Filtro de busqueda</h6>
            </div>
            <div class="card-body">
                <div class="row">

                    <div class="form-group col-sm-4">
                        <label asp-for="IdRegion" class="control-label"></label>
                        <div class="input-group">
                            <div class="input-group-append">
                                <label class="input-group-text" for="selectRegion">
                                    <i class="bi bi-building"></i>
                                </label>
                            </div>
                            <select id="selectRegion" asp-for="IdRegion" class="form-control" onchange="return changeRegion();" asp-items=@ViewBag.Regiones></select>
                        </div>
                    </div>


                    <div class="form-group col-sm-4">
                        <label class="control-label">Inmueble</label>
                        <input placeholder="Inmueble" id="inputDataListInm" type="text" list="datalistIdInmueble" class="form-control" />
                        <datalist id="datalistIdInmueble"></datalist>
                        <select id="inputIdInmueble" class="form-control" style="visibility:hidden; height:0px"></select>
                    </div>


                   <div class="form-group col-sm-4">
                        <label asp-for="IdConcepto" class="control-label"></label>
                        <div class="input-group">
                            <div class="input-group-append">
                                <label class="input-group-text" for="selectIdConcepto">
                                    <i class="bi bi-building"></i>
                                </label>
                            </div>
                            <select id="selectIdConcepto" asp-for="IdConcepto" class="form-control" asp-items=@ViewBag.Conceptos></select>
                        </div>
                        <span asp-validation-for="IdConcepto" class="text-danger"></span>
                    </div> 

                </div>
                <div class="container">
                    <div class="row">
                        <div class="form-group col-sm-4">
                            <button type="submit" value="Buscar" class="btn btn-success btn-sm">
                                <i class="bi bi-floppy2"></i>
                                Buscar
                            </button>
                        </div>
                    </div>

                    <div class="row" id="Descargar">
                        <div class="form-group col-sm-4">
                            <button onclick="Descargar();"  id="Descargar" name="Descargar" value="Descargar" class="btn btn-warning btn-sm">
                                <i class="bi bi-floppy2"></i>
                                Descargar
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </div>
</form>
<input asp-action="DescargaExcel" asp-controller="Facturas" type="url" id="urlDescargar" name="urlDescargar" hidden />
<hr />
<div id="tableData" name="tableData" class="table-responsive">
    @{
        try
        {
            await Html.RenderPartialAsync("_Listado");
        }
        catch (Exception)
        {
        }
    }
</div>

@section Scripts {
    <script src="~/lib/JQuery_Versiones/jquery_2.2.4/dist/jquery.min.js"></script>

    <script src="~/js/sweetalert.min.js"></script>
    <link href="~/css/sweetalert.css" rel="stylesheet">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>

        @{
            await Html.RenderPartialAsync("_ValidationScriptsPartial");
        }
}

<script src="~/js/FacturasPagadas.js" />
